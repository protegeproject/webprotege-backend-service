<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!--&lt;!&ndash; Choose the logs directory path based on operating system &ndash;&gt;-->
    <!--<if condition='property("os.name").startsWith("Windows")'>-->
        <!--<then>-->
            <!--&lt;!&ndash; Windows gets \ProgramData\WebProtege\Logs&ndash;&gt;-->
            <!--<property name="logsdir" value="${logs.directory:-/ProgramData/WebProtege/Logs}"/>-->
        <!--</then>-->
        <!--<else>-->
            <!--&lt;!&ndash; Everything else, e.g. MacOS and Linux gets /var/log/webprotege &ndash;&gt;-->
            <!--<property name="logsdir" value="${logs.directory:-/var/log/webprotege}"/>-->
        <!--</else>-->
    <!--</if>-->

    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}  %-5level  %highlight(%msg) %n</Pattern>
        </encoder>
    </appender>

    <appender name="webprotege" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logsdir}/webprotege.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>${logsdir}/webprotege.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %msg%n</Pattern>
        </encoder>
    </appender>

    <appender name="webprotege-all" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logsdir}/webprotege-all.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>${logsdir}/webprotege-all.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %msg%n</Pattern>
        </encoder>
    </appender>


    <logger name="edu.stanford.bmir.protege.web" level="info">
        <appender-ref ref="webprotege"/>
        <appender-ref ref="stdout"/>
    </logger>

    <turboFilter class="ch.qos.logback.classic.turbo.MarkerFilter">
        <Name>BrowsingFilter</Name>
        <Marker>BROWSING</Marker>
        <OnMatch>DENY</OnMatch>
    </turboFilter>


    <root level="info">
        <appender-ref ref="webprotege-all"/>
    </root>

</configuration>
